spring:
  application:
    name: franchise-backend-system
  data:
    mongodb:
      auto-index-creation: true
  mongodb:
    uri: ${MONGODB_URI:mongodb+srv://${MONGODB_USER}:${MONGODB_PASS}@cluster0.xu0ajse.mongodb.net/franchise_backend?retryWrites=true&w=majority&appName=Cluster0}

server:
  port: ${SERVER_PORT:8080}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
